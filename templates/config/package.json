{
    "name": "<%= name %>",
    "publisher": "<%= publisher %>",
    "author": "<%= author %>",
    "keywords": [
        "cloudide",
        "plugin"
    ],
    "version": "0.0.1",
    "license": "SEE LICENSE IN LICENSE",
    "description": "<%= description %>",
    "displayName": "<%= name %>",
    "icon": "resources/icons/logo.png",
    "files": [
        "src"
    ],
    "main": "dist/plugin.js",
    "activationEvents": [<% if(type == 'webview') { %>
        "*"<% } else { %> 
        "onCommand:plugin.sayHello"<% } %>
    ],<% if(type == 'simple') { %>
    "contributes": {
		"commands": [
			{
				"command": "plugin.sayHello",
				"title": "Say Hello"
			}
		]
	},<% } %> 
    "devDependencies": {
        "@codearts/plugin": "latest",
        "@cloudide/plugin-packager": "latest",
        "rimraf": "2.6.2",
        "ts-loader": "^6.0.4",
        "typescript": "^3.9.7",
        "typescript-formatter": "7.2.2"<% if(type == 'webview') { %>,
        "webpack": "^4.39.2",
        "webpack-cli": "^3.3.7"<% } %>
    },
    "dependencies": {
        "@cloudide/nls": "latest"<% if(type == 'webview') { %>,
        "@codearts/core": "latest",
        "@cloudide/messaging": "latest"<% } %>
    },
    "scripts": {
        "prepare": "npm run clean && npm run build",
        "clean": "rimraf dist",
        "format-code": "tsfmt -r",
        "watch": "tsc -watch",
        "compile": "tsc",
        "pack": "cloudide-pack -p",<% if(type == 'webview') { %>
        "build": "npm run format-code && npm run compile && webpack",
        "build-browser": "webpack --config webpack.config.js"<% } else { %>
        "build": "npm run format-code && npm run compile"<% } %>
    },
    "engines": {
        "codearts": "^1.0.0"
    }
}
